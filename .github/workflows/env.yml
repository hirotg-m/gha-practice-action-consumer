name: Environment variables
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    env:
      ENV1: value1
      ENV2: value2
    steps:
      - name: Print environment variables
        run: |
          echo "ENV1 - ${ENV1}"
          echo "ENV2 - ${ENV2}"